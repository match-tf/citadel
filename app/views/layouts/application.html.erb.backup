<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title><%= content_for :title %> - match.tf</title>
	<meta name="description" content="match.tf - a web platform for hosting TF2 tournaments of any size">
	<meta property="og:title" content="<%= content_for :title %> - match.tf"/>
	<meta property="og:description" content="match.tf - a web platform for hosting TF2 tournaments of any size"/>
	<meta property="og:type" content="website"/>
	<meta property="og:image" content="<%= asset_path 'ogimage.png' %>"/>
	<meta name="theme-color" content="#e39418">
	<link rel="icon" sizes="192x192" href="<%= asset_path 'icon-192.png' %>">
	<%= csrf_meta_tags %> 	
	<%= stylesheet_link_tag 'application' %>  
	<%= javascript_include_tag 'application'%>  	
	<!--Custom Font-->
	<link href="https://fonts.googleapis.com/css?family=Anton|Lato" rel="stylesheet">
</head>
<body>
	<nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
		<div class="container-fluid">
			<div class="navbar-header">
				<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#sidebar-collapse"><span class="sr-only">Toggle navigation</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span></button>
				<a class="navbar-brand" href="<%= pages_home_path %>"><span>MATCH</span>.TF</a>
				<ul class="nav navbar-top-links navbar-right">
				<% if user_signed_in? %>
					<%= render 'notifications', notifications: @notifications %>
				<%else %>
					<%= render 'user_signin' %>	
				<% end %>
				</ul>
			</div>
		</div><!-- /.container-fluid -->
	</nav>
	<div id="sidebar-collapse" class="col-sm-3 col-lg-2 sidebar animated fadeInDown">
		<% if user_signed_in? %>
		<div class="profile-sidebar">
			<div class="profile-userpic">
				<img src="<%= current_user.avatar.icon.url %>" class="img-responsive" alt="">
			</div>
			<div class="profile-usertitle">
				<div class="profile-usertitle-name"><a href="<%= user_path(current_user)%>"><%= current_user.name %></a></div>
			</div>
			<div class="clear"></div>
		</div>
		<div class="divider"></div>
		<% end %>
		<ul class="nav menu">
		<%= render "navbar" %>
		</ul>
		<% if user_signed_in? %>
			<%= render 'user_dropdown' %>
		<% end %>	
	</div><!--/.sidebar-->
		
	<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">
	<div class="row">	
	<%= render 'flashes' %>
	</div>
	<%= yield %>
	<%= render 'admin_bar' %>
	</div>	
		
</body>
</html>