.form-group
  %label.control-label Team Limit (0 for no limit)
  = number_field_tag 'single_elimination_tournament[teams_limit]',
                     single_elimination_tournament[:teams_limit],
                     min: 0, class: 'form-control'

.form-group
  %label.control-label Starting Round (Will ignore any rounds before this)
  = number_field_tag 'single_elimination_tournament[starting_round]',
                     single_elimination_tournament[:starting_round],
                     min: 0, class: 'form-control'

.form-group
  %p A single-elimination, knockout, or sudden death tournament is a type of elimination tournament where the loser of each match-up is immediately eliminated from the tournament. Each winner will play another in the next round, until the final match-up, whose winner becomes the tournament champion.