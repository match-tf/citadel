%h2.outside Post Edits for #{present(@match).link}

.panel.panel-container
  .row
    .panel.panel-default
      - @edits.each do |comm_edit|
        = render 'comm_edit', match: @match, comm: comm_edit
