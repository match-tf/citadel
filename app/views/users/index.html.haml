- content_for(:title) { 'Users' }

.row.row-header
  %h1.tab-header.outside Users
  .col-sm-3.col-md-3.searchbox.pull-right
    = render 'search'

.panel.panel-container
  .row
    .panel.panel-default
      - if @users.empty?
        .panel-body No users found...
    
      - else
        %ul.list-group
          - @users.each do |user|
            %li.list-group-item
              = render 'users/listing', user: user
    
    = bootstrap_paginate @users
