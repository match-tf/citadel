- user = present(user)

.row.slim-gutter
  .pull-left= user.avatar_tag

  .user-details
    .row.slim-gutter
      = user.link
      = user.titles team: local_assigns[:team], league: local_assigns[:league]
      .steam-link= user.steam_link
